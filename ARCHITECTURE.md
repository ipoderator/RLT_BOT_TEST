# Архитектура проекта RLT Bot

## Оглавление

1. [Обзор системы](#обзор-системы)
2. [Компоненты системы](#компоненты-системы)
3. [Поток данных](#поток-данных)
4. [Структура базы данных](#структура-базы-данных)
5. [Безопасность](#безопасность)
6. [Технологический стек](#технологический-стек)
7. [Развертывание](#развертывание)

---

## Обзор системы

**RLT Bot** — это Telegram-бот для аналитики статистики видео-креаторов. Система принимает запросы на естественном языке (русский) и возвращает числовые результаты, используя генерацию SQL-запросов через LLM (GigaChat).

### Основные возможности

- 📊 Анализ статистики видео (просмотры, лайки, комментарии, репосты, сохранения)
- 🔍 Запросы на естественном языке на русском
- 📈 Поддержка итоговой статистики и динамики (почасовые снапшоты)
- 🤖 Автоматическая генерация SQL-запросов с помощью LLM
- 📁 Анализ загруженных JSON-файлов через GigaChat

### Архитектурные принципы

- **Модульность**: Каждый компонент отвечает за свою задачу
- **Асинхронность**: Использование async/await для эффективной работы с БД и API
- **Безопасность**: Валидация всех SQL-запросов перед выполнением
- **Расширяемость**: Легко добавить новые типы запросов или источники данных

---

## Компоненты системы

### 1. Telegram Bot (`bot.py`)

**Назначение**: Точка входа в систему, обработка сообщений пользователей.

**Основные функции**:
- Прием и обработка команд (`/start`, `/clear_file`)
- Обработка текстовых запросов пользователей
- Загрузка и обработка JSON-файлов
- Управление сессиями и кэшированием

**Ключевые классы и функции**:
```python
- Bot, Dispatcher (aiogram) - обработка Telegram API
- VideoAnalytics - основной класс для работы с аналитикой
- FileAnalyzer - анализ загруженных файлов
```

**Обработчики команд**:
- `/start` - инициализация бота
- `/clear_file` - очистка загруженного файла
- `/total_videos`, `/total_views` - быстрые команды для частых запросов

### 2. Query Generator (`query_generator.py`)

**Назначение**: Преобразование вопросов на естественном языке в SQL-запросы.

**Основные компоненты**:
- `SQLQueryGenerator` - класс для генерации SQL
- `DATABASE_SCHEMA` - описание схемы БД для промпта
- `SYSTEM_PROMPT` - системный промпт для GigaChat

**Процесс генерации**:
1. Нормализация запроса (удаление лишних пробелов, нормализация чисел)
2. Формирование промпта с описанием схемы БД и примерами
3. Отправка запроса в GigaChat API
4. Очистка ответа (удаление markdown, комментариев)
5. Возврат чистого SQL-запроса

**Особенности**:
- Поддержка русских дат ("28 ноября 2025" → `DATE '2025-11-28'`)
- Нормализация чисел с пробелами ("100 000" → `100000`)
- Обработка диапазонов дат ("с 1 по 5 ноября включительно")

### 3. Query Executor (`query_executor.py`)

**Назначение**: Выполнение SQL-запросов и извлечение результатов.

**Основной класс**: `VideoAnalytics`

**Ключевые методы**:
- `answer_question(question: str)` - главный метод для ответа на вопрос
- `_validate_sql(sql: str)` - валидация SQL на безопасность
- `_execute_query(sql: str)` - выполнение запроса через asyncpg
- `_format_number(number)` - форматирование результата

**Безопасность**:
- Разрешены только SELECT-запросы
- Запрещены: DROP, DELETE, UPDATE, INSERT, ALTER, CREATE, TRUNCATE
- Проверка выполняется перед каждым запросом

**Управление соединениями**:
- Использование пула соединений asyncpg (min_size=1, max_size=5)
- Автоматическое управление жизненным циклом соединений

### 4. Database Models (`database.py`)

**Назначение**: Определение моделей данных и инициализация БД.

**Модели данных**:

#### Video
Итоговая статистика по каждому видео:
- `id` (Integer, PK)
- `creator_id` (String, indexed)
- `video_created_at` (DateTime)
- `views_count`, `likes_count`, `comments_count`, `reports_count` (BigInteger)
- `created_at`, `updated_at` (DateTime)

#### VideoSnapshot
Почасовые снапшоты для отслеживания динамики:
- `id` (Integer, PK)
- `video_id` (Integer, FK → videos.id)
- `views_count`, `likes_count`, `comments_count`, `reports_count` (BigInteger)
- `delta_views_count`, `delta_likes_count`, `delta_comments_count`, `delta_reports_count` (BigInteger)
- `created_at` (DateTime, indexed)

**Функции**:
- `init_db(database_url)` - создание всех таблиц
- `get_session(engine)` - создание сессии SQLAlchemy

### 5. File Analyzer (`file_analyzer.py`)

**Назначение**: Анализ загруженных JSON-файлов через GigaChat.

**Основной класс**: `FileAnalyzer`

**Функциональность**:
- Загрузка и кэширование JSON-файлов
- Анализ структуры данных через GigaChat
- Ответы на вопросы по содержимому файла
- Управление кэшем файлов

**Кэширование**:
- Файлы кэшируются по хешу содержимого
- Метаданные хранятся в `cache/metadata.json`
- Автоматическая очистка старых файлов

### 6. Data Loader (`load_data.py`)

**Назначение**: Загрузка данных из JSON-файлов в базу данных.

**Процесс загрузки**:
1. Парсинг JSON-файла
2. Создание записей в таблице `videos`
3. Создание снапшотов в таблице `video_snapshots`
4. Вычисление дельт (приращений) между снапшотами

**Поддержка**:
- Загрузка из локального файла
- Загрузка из URL (скачивание файла)

---

## Поток данных

### Сценарий 1: Ответ на вопрос пользователя

```
┌─────────────┐
│   Пользователь   │
│  (Telegram)      │
└────────┬─────────┘
         │ "Сколько всего видео?"
         ▼
┌─────────────────┐
│   bot.py        │
│  (обработчик)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ VideoAnalytics  │
│ answer_question │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ SQLQueryGenerator│
│ generate_sql()  │
└────────┬────────┘
         │
         │ Промпт + вопрос
         ▼
┌─────────────────┐
│   GigaChat API  │
│   (LLM)         │
└────────┬────────┘
         │ SQL запрос
         ▼
┌─────────────────┐
│ VideoAnalytics  │
│ _validate_sql() │
└────────┬────────┘
         │
         │ Валидный SQL
         ▼
┌─────────────────┐
│   PostgreSQL    │
│   (asyncpg)     │
└────────┬────────┘
         │ Результат (число)
         ▼
┌─────────────────┐
│ VideoAnalytics  │
│ _format_number()│
└────────┬────────┘
         │ "42"
         ▼
┌─────────────────┐
│   bot.py        │
│  (ответ)        │
└────────┬────────┘
         │
         ▼
┌─────────────┐
│ Пользователь │
└─────────────┘
```

### Сценарий 2: Загрузка и анализ JSON-файла

```
┌─────────────┐
│   Пользователь   │
│  (Telegram)      │
└────────┬─────────┘
         │ Загрузка файла
         ▼
┌─────────────────┐
│   bot.py        │
│  (обработчик)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  FileAnalyzer   │
│  load_file()    │
└────────┬────────┘
         │
         │ Сохранение в кэш
         ▼
┌─────────────────┐
│   cache/        │
│   (локальное)   │
└────────┬────────┘
         │
         │ Вопрос по файлу
         ▼
┌─────────────────┐
│  FileAnalyzer   │
│  analyze()      │
└────────┬────────┘
         │
         │ Промпт + данные
         ▼
┌─────────────────┐
│   GigaChat API  │
│   (LLM)         │
└────────┬────────┘
         │ Ответ
         ▼
┌─────────────────┐
│   bot.py        │
│  (ответ)        │
└────────┬────────┘
```

### Сценарий 3: Загрузка данных в БД

```
┌─────────────────┐
│   JSON файл     │
│  (локальный/URL)│
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  load_data.py   │
│  parse_json()   │
└────────┬────────┘
         │
         │ Данные видео
         ▼
┌─────────────────┐
│  database.py    │
│  Video model    │
└────────┬────────┘
         │
         │ INSERT
         ▼
┌─────────────────┐
│   PostgreSQL    │
│   (videos)      │
└────────┬────────┘
         │
         │ Снапшоты
         ▼
┌─────────────────┐
│  database.py    │
│VideoSnapshot    │
└────────┬────────┘
         │
         │ INSERT + вычисление дельт
         ▼
┌─────────────────┐
│   PostgreSQL    │
│(video_snapshots)│
└─────────────────┘
```

---

## Структура базы данных

### ER-диаграмма (текстовая)

```
┌─────────────────┐
│     videos      │
├─────────────────┤
│ id (PK)         │◄──────┐
│ creator_id      │       │
│ video_created_at│       │
│ views_count     │       │
│ likes_count     │       │
│ comments_count  │       │
│ reports_count   │       │
│ created_at      │       │
│ updated_at      │       │
└─────────────────┘       │
                          │
                          │ video_id (FK)
                          │
┌─────────────────────────┐
│   video_snapshots       │
├─────────────────────────┤
│ id (PK)                 │
│ video_id (FK) ──────────┘
│ views_count             │
│ likes_count             │
│ comments_count          │
│ reports_count           │
│ delta_views_count       │
│ delta_likes_count       │
│ delta_comments_count    │
│ delta_reports_count     │
│ created_at (indexed)    │
│ updated_at              │
└─────────────────────────┘
```

### Индексы

- `videos.creator_id` - для быстрого поиска по креатору
- `video_snapshots.video_id` - для JOIN операций
- `video_snapshots.created_at` - для фильтрации по датам

### Использование таблиц

**Таблица `videos`**:
- Итоговая статистика по каждому видео
- Используется для запросов типа "сколько всего видео", "сумма просмотров"
- Один видео = одна запись

**Таблица `video_snapshots`**:
- Почасовые снапшоты статистики
- Используется для запросов о динамике и приросте
- Один видео = множество снапшотов (каждый час)
- Поля `delta_*_count` содержат приращения с предыдущего замера

---

## Безопасность

### Валидация SQL-запросов

Все SQL-запросы проходят проверку перед выполнением:

**Разрешено**:
- ✅ SELECT запросы
- ✅ Агрегатные функции (COUNT, SUM, MAX, MIN, AVG)
- ✅ JOIN операции
- ✅ Подзапросы (subqueries)
- ✅ WHERE, GROUP BY, HAVING, ORDER BY

**Запрещено**:
- ❌ DROP, DELETE, UPDATE, INSERT
- ❌ ALTER, CREATE, TRUNCATE
- ❌ EXEC, EXECUTE
- ❌ GRANT, REVOKE
- ❌ Любые операции изменения данных или структуры

### Реализация валидации

```python
def _validate_sql(self, sql: str) -> bool:
    sql_upper = sql.upper().strip()
    
    # Запрещенные операции
    forbidden = [
        'DROP', 'DELETE', 'UPDATE', 'INSERT', 'ALTER', 
        'CREATE', 'TRUNCATE', 'EXEC', 'EXECUTE', 
        'GRANT', 'REVOKE'
    ]
    
    for keyword in forbidden:
        if keyword in sql_upper:
            return False
    
    # Разрешаем только SELECT
    if not sql_upper.startswith('SELECT'):
        return False
    
    return True
```

### Переменные окружения

Чувствительные данные хранятся в переменных окружения:
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `GIGACHAT_CREDENTIALS` - ключ авторизации GigaChat
- `DATABASE_URL` - строка подключения к БД (с паролем)

Файл `.env` не должен попадать в репозиторий (добавлен в `.gitignore`).

---

## Технологический стек

### Backend

- **Python 3.11+** - основной язык программирования
- **aiogram 3.x** - асинхронный фреймворк для Telegram Bot API
- **SQLAlchemy** - ORM для работы с БД
- **asyncpg** - асинхронный драйвер PostgreSQL
- **GigaChat API** - LLM для генерации SQL-запросов

### База данных

- **PostgreSQL 15** - реляционная БД
- Поддержка индексов для оптимизации запросов
- Использование пула соединений для эффективности

### Инфраструктура

- **Docker** - контейнеризация приложения
- **Docker Compose** - оркестрация контейнеров
- **loguru** - логирование с ротацией и архивацией

### Утилиты

- **python-dotenv** - управление переменными окружения
- **requests** - HTTP-запросы для загрузки файлов

---

## Развертывание

### Docker Compose

Проект использует Docker Compose для развертывания:

**Сервисы**:
1. **postgres** - PostgreSQL база данных
2. **bot** - Telegram бот приложение

**Сеть**: `rlt_network` (bridge)

**Volumes**:
- `postgres_data` - данные PostgreSQL
- `./logs` → `/app/logs` - логи приложения
- `./cache` → `/app/cache` - кэш файлов

### Переменные окружения

Необходимые переменные (в файле `.env`):

```env
TELEGRAM_BOT_TOKEN=your_token
GIGACHAT_CREDENTIALS=your_credentials
GIGACHAT_SCOPE=GIGACHAT_API_PERS
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=video_analytics
POSTGRES_PORT=5432
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/video_analytics
```

### Запуск

```bash
# Запуск всех сервисов
docker-compose up -d

# Применение миграций
docker-compose exec bot python -c "from database import init_db; import os; from dotenv import load_dotenv; load_dotenv(); init_db(os.getenv('DATABASE_URL'))"

# Загрузка данных
docker-compose exec bot python load_data.py <file.json>

# Просмотр логов
docker-compose logs -f bot
```

---

## Структура проекта

```
rlt/
├── bot.py                 # Telegram бот (точка входа)
├── query_generator.py      # Генерация SQL из естественного языка
├── query_executor.py       # Выполнение SQL запросов
├── database.py             # Модели данных SQLAlchemy
├── file_analyzer.py        # Анализ JSON файлов
├── load_data.py            # Загрузка данных в БД
├── check_db_compliance.py  # Проверка соответствия БД
├── setup_env.py            # Настройка окружения
├── fix_all_issues.py       # Автоматическое исправление проблем
├── fix_dependencies.py     # Исправление зависимостей
├── run_all_tests.py        # Запуск всех тестов
├── test_*.py               # Тестовые скрипты
├── migrations/             # SQL миграции
│   ├── 001_initial_schema.sql
│   └── README.md
├── logs/                   # Логи приложения
├── cache/                  # Кэш файлов
├── Dockerfile              # Образ для бота
├── docker-compose.yml      # Конфигурация Docker Compose
├── requirements.txt        # Python зависимости
├── README.md               # Основная документация
└── ARCHITECTURE.md         # Этот файл
```

---

## Расширение системы

### Добавление новых типов запросов

1. Обновить `DATABASE_SCHEMA` в `query_generator.py`
2. Добавить примеры в `SYSTEM_PROMPT`
3. При необходимости обновить валидацию в `query_executor.py`

### Добавление новых команд бота

1. Добавить обработчик в `bot.py`
2. Зарегистрировать команду в `set_bot_commands()`
3. Обновить документацию

### Оптимизация производительности

- Добавление индексов в БД для частых запросов
- Кэширование результатов запросов
- Оптимизация промптов для GigaChat

---

## Заключение

Архитектура проекта построена на принципах модульности, безопасности и расширяемости. Каждый компонент имеет четко определенную ответственность, что упрощает поддержку и развитие системы.

Для вопросов и предложений по архитектуре см. [README.md](README.md).

