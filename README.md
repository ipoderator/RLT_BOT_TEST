# Telegram-бот для аналитики по видео

Бот для аналитики статистики видео-креаторов на основе запросов на естественном языке.

## Возможности

- 📊 Анализ статистики видео (просмотры, лайки, комментарии, репосты, сохранения)
- 🔍 Запросы на естественном языке на русском
- 📈 Поддержка итоговой статистики и динамики (почасовые снапшоты)
- 🤖 Автоматическая генерация SQL запросов с помощью LLM

## Установка

1. Клонируйте репозиторий и перейдите в директорию проекта:
```bash
cd rlt
```

2. Создайте виртуальное окружение (если еще не создано):
```bash
python3 -m venv env
source env/bin/activate  # для macOS/Linux
# или
env\Scripts\activate  # для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:

   **Вариант 1 (интерактивный):**
   ```bash
   python setup_env.py
   ```
   Скрипт поможет заполнить все необходимые переменные.

   **Вариант 2 (вручную):**
   ```bash
   cp .env.example .env
   ```
   Затем отредактируйте `.env` файл и заполните значения:
   ```env
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
   GIGACHAT_CREDENTIALS=your_gigachat_credentials_here
   GIGACHAT_SCOPE=GIGACHAT_API_PERS
   DATABASE_URL=postgresql://user:password@localhost:5432/video_analytics
   ```

   Где получить:
   - **TELEGRAM_BOT_TOKEN**: Получите у [@BotFather](https://t.me/BotFather) в Telegram
   - **GIGACHAT_CREDENTIALS**: Получите на [платформе разработчиков Сбера](https://developers.sber.ru/gigachat) - создайте приложение и получите Authorization Key
   - **GIGACHAT_SCOPE**: Область действия API (GIGACHAT_API_PERS для личного использования, GIGACHAT_API_B2B или GIGACHAT_API_CORP для бизнеса)
   - **DATABASE_URL**: URL базы данных PostgreSQL в формате `postgresql://user:password@host:port/dbname`

6. Убедитесь, что PostgreSQL установлен и запущен. Создайте базу данных:
```sql
CREATE DATABASE video_analytics;
```

## Подготовка данных

1. Скачайте JSON файл с данными (ссылка должна быть предоставлена отдельно)

2. Загрузите данные в базу:
```bash
python load_data.py <путь_к_json_файлу>
```

Например:
```bash
python load_data.py data.json
```

## Запуск бота

```bash
python bot.py
```

После запуска бот будет отвечать на сообщения в Telegram.

## Использование

1. Найдите вашего бота в Telegram (по имени, которое вы указали при создании у @BotFather)

2. Отправьте команду `/start` для начала работы

3. Задавайте вопросы на русском языке, например:
   - "Сколько всего видео есть в системе?"
   - "Сколько видео у креатора с id creator123 вышло с 1 ноября 2025 по 5 ноября 2025 включительно?"
   - "Сколько видео набрало больше 100 000 просмотров за всё время?" (поддерживаются пробелы в числах)
   - "На сколько просмотров в сумме выросли все видео 28 ноября 2025?"
   - "Сколько разных видео получали новые просмотры 27 ноября 2025?"

4. Бот вернет ответ - одно число с результатом запроса (например: "42", "1000", "0")

## Тестирование примеров из ТЗ

Для проверки конкретных примеров из технического задания запустите:

```bash
python test_examples.py
```

Этот скрипт проверит все примеры вопросов и убедится, что бот возвращает корректные ответы в виде чисел.

## Структура проекта

```
rlt/
├── bot.py                 # Основной файл Telegram бота
├── database.py            # Модели базы данных (SQLAlchemy)
├── load_data.py           # Скрипт для загрузки данных из JSON
├── query_generator.py     # Модуль генерации SQL из естественного языка
├── query_executor.py      # Модуль выполнения SQL запросов
├── requirements.txt       # Зависимости проекта
├── .env.example          # Пример файла с переменными окружения
└── README.md             # Этот файл
```

## Структура базы данных

### Таблица `videos`
Итоговая статистика по каждому видео:
- `id` - идентификатор видео (PRIMARY KEY)
- `creator_id` - идентификатор креатора
- `video_created_at` - дата и время публикации видео
- `views_count` - финальное количество просмотров
- `likes_count` - финальное количество лайков
- `comments_count` - финальное количество комментариев
- `reports_count` - финальное количество жалоб
- `created_at`, `updated_at` - служебные поля

### Таблица `video_snapshots`
Почасовые снапшоты статистики для отслеживания динамики:
- `id` - идентификатор снапшота (PRIMARY KEY)
- `video_id` - ссылка на соответствующее видео (FOREIGN KEY)
- `views_count`, `likes_count`, `comments_count`, `reports_count` - текущие значения на момент замера
- `delta_views_count`, `delta_likes_count`, `delta_comments_count`, `delta_reports_count` - приращения с прошлого замера
- `created_at` - время замера (раз в час)
- `updated_at` - служебное поле

## Технологии

- **Python 3.11+**
- **aiogram** - для работы с Telegram API (асинхронный фреймворк)
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - база данных
- **asyncpg** - асинхронный драйвер для PostgreSQL
- **GigaChat API** - для генерации SQL запросов из естественного языка (работает в России без VPN)

## Устранение проблем

Если возникают ошибки при запуске, см. [TROUBLESHOOTING.md](TROUBLESHOOTING.md) или [QUICK_FIX.md](QUICK_FIX.md)

**Быстрое исправление всех проблем:**
```bash
python fix_all_issues.py
```

**Или отдельно:**
- Исправление зависимостей: `python fix_dependencies.py`
- Настройка окружения: `python setup_env.py`

## Тестирование

Проект включает комплексные скрипты для проверки всех требований из технического задания.

### Быстрый запуск всех проверок

```bash
python run_all_tests.py
```

Этот скрипт запускает все проверки последовательно и выводит итоговый отчет.

### Отдельные скрипты проверки

1. **Проверка структуры БД:**
   ```bash
   python test_database_structure.py
   ```

2. **Проверка генерации SQL:**
   ```bash
   python test_sql_generation.py
   ```

3. **Тестирование примеров вопросов:**
   ```bash
   python test_bot.py
   ```

4. **Комплексная проверка всех требований:**
   ```bash
   python test_requirements.py
   ```

Подробное описание всех скриптов тестирования см. в [TESTING.md](TESTING.md).

## Примечания

- Бот использует GigaChat для генерации SQL запросов (работает в России без VPN)
- Для расчета прироста используются данные из таблицы `video_snapshots` (можно использовать готовые поля `delta_*_count` или вычислять разницу между MAX и MIN)
- Все SQL запросы валидируются на безопасность (запрещены операции типа DROP, DELETE, UPDATE и т.д.)
- Бот поддерживает обработку дат в русском формате: "28 ноября 2025", "с 1 по 5 ноября 2025 включительно" и т.д.
- Бот возвращает только одно число в ответе (без дополнительного текста)
- Контекст диалога не хранится - каждый запрос обрабатывается независимо

## Лицензия

Проект создан для тестового задания.

